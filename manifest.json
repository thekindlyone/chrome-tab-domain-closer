{
  "manifest_version": 2,

  "name": "Tab_domain_closer",
  "description": "This extension closes all tabs that are on the same domain as current tab",
  "version": "1.0",

  "browser_action": {
    "default_icon": "icon.png"
  },
   "background": {
    "scripts": ["eventPage.js"],
    "persistent": false
  },
  "permissions": [
    "activeTab",
    "tabs"
  ]
}